<UserControl x:Class="PosTest.Views.SubViews.WaitingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:subvm="clr-namespace:PosTest.ViewModels.SubViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox x:Name="OrdersList" ItemsSource="{Binding Orders}" 
                         SelectedItem="{Binding DisplayedOrder}"
                         ItemTemplate="{StaticResource OrderButtonList_ItemTemplate}"
                         ItemContainerStyle="{StaticResource OrderButtonList_ItemStyle}"
                         HorizontalContentAlignment="Left"
                         VerticalContentAlignment="Center">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>
