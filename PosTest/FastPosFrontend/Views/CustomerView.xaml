<UserControl  x:Class="FastPosFrontend.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialdesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:caliburn="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:FastPosFrontend.ViewModels"
             xmlns:model="clr-namespace:ServiceInterface.Model;assembly=ServiceInterface"
             xmlns:subviews="clr-namespace:FastPosFrontend.Views.SubViews"
             xmlns:Controls="clr-namespace:FastPosFrontend.SL.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400" x:Name="CustomerViewUc"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase" >

    <UserControl.Resources>
            
    </UserControl.Resources>

    <materialdesign:Flipper 
        IsFlipped="{Binding IsEditing}"   >
        <materialdesign:Flipper.BackContent>
            <subviews:CustomerDetailView DataContext="{Binding CustomerDetailVm,diag:PresentationTraceSources.TraceLevel=High}" />

            <!--<subviews:CustomerDetailView caliburn:View.Model="{Binding CustomerDetailVm}"></subviews:CustomerDetailView>-->
            

        </materialdesign:Flipper.BackContent>
        <materialdesign:Flipper.FrontContent>
            <StackPanel >

                <!--<Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>-->
                <materialdesign:ColorZone 
                    Mode="PrimaryMid" 
                    Grid.Row="0" 
                    Margin="0 0 0 20" 
                    CornerRadius="0" 
                    materialdesign:ShadowAssist.ShadowDepth="Depth2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="8 8 8 3" Grid.Row="0" >
                            <Border Background="White" MaxHeight="30" CornerRadius="3" Margin="0 0 50 0" ToolTip="Enter to search, ignore case">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button Style="{DynamicResource MaterialDesignToolButton}"
                                        x:Name="SearchButton" Height="24" Width="24">
                                        <materialdesign:PackIcon Kind="Magnify" Opacity=".56" />
                                    </Button>
                                    <!--KeyDown="Search_OnKeyDown"-->
                                    <TextBox Grid.Column="1" Margin="5 0 0 0"  FontSize="18" FontWeight="Bold"
                                         x:Name="SearchBox"
                                         materialdesign:TextFieldAssist.DecorationVisibility="Hidden" BorderThickness="0"
                                         MinWidth="230" VerticalAlignment="Center" 
                                         Text="{Binding FilterString, UpdateSourceTrigger=PropertyChanged}"
                                         Foreground="Black"/>
                                </Grid>
                            </Border>

                            <Button  Margin="4 0 4 0" Padding="3" HorizontalAlignment="Right" >
                                <materialdesign:PackIcon Kind="ViewGridOutline"  Width="25" Height="25" Foreground="White" />
                            </Button>

                            <Button  Margin="4 0 4 0" Padding="3">
                                <materialdesign:PackIcon Kind="ViewList" Width="25" Height="25" Foreground="White"/>
                            </Button>

                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="3 0 3 3">
                            <StackPanel.Resources>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=SearchBox, Path=Text}" Value="">
                                            <Setter Property="Visibility" Value="Hidden"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Resources>
                            <Button Margin="5" Height="40" caliburn:Message.Attach="[Event Click] = [Action CreateAndSave()]" Foreground="White" Style="{StaticResource MaterialDesignFlatDarkBgButton}">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="Create &amp; Save"  FontWeight="Thin"></TextBlock>
                                    <TextBlock Text="{Binding FilterString}"  FontSize="10" HorizontalAlignment="Center"></TextBlock>
                                </StackPanel>
                            </Button>
                            <Button Margin="5" Height="40"  caliburn:Message.Attach="[Event Click] = [Action CreateAndEdit()]" Foreground="White" Style="{StaticResource MaterialDesignFlatDarkBgButton}">
                                <StackPanel Orientation="Vertical" >
                                    <TextBlock Text="Create &amp; Edit"  FontWeight="Thin"></TextBlock>
                                    <TextBlock Text="{Binding FilterString}"  FontSize="10" HorizontalAlignment="Center"></TextBlock>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </materialdesign:ColorZone>

                <ListBox    
                Grid.Row="1"  
                Visibility="Visible" Background="White" 
                ItemsSource="{Binding CustomerCollectionViewSource.View}">

                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2" ></UniformGrid>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate  DataType="model:Customer">
                            <Button Height="60" Background="YellowGreen" 
                                Command="{Binding ElementName=CustomerViewUc,Path=DataContext.SelectCustomerCommand}" 
                                CommandParameter="{Binding}" >
                                <StackPanel    Width="120"   Orientation="Vertical"  >
                                    <TextBlock  FontSize="14" Text="{Binding Name}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" />
                                    <TextBlock  FontSize="14" Text="{Binding FirstNumber}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </materialdesign:Flipper.FrontContent>
    </materialdesign:Flipper>


</UserControl>
