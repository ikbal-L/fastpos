<UserControl x:Class="FastPosFrontend.SL.Controls.PlaceHolder.ItemsControlPlaceHolder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FastPosFrontend.SL.Controls.PlaceHolder"
             xmlns:converters="clr-namespace:FastPosFrontend.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!--<UserControl.Template>
        <ControlTemplate TargetType="local:ItemsControlPlaceHolder">
          
        </ControlTemplate>
        </UserControl.Template>-->

    <Grid>
        <ContentPresenter x:Name="ItemsControlPresenter" />
        <TextBlock x:Name="PlaceHolderTextBlock" Text="{Binding RelativeSource={RelativeSource AncestorType=local:ItemsControlPlaceHolder},Path=PlaceHolderText}"
                           Style="{Binding RelativeSource={RelativeSource AncestorType=local:ItemsControlPlaceHolder},Path=PlaceholderTextStyle}"/>
    </Grid>


</UserControl>
